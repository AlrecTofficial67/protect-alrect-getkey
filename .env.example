# =============================================
# PROTECT by Alrect GET Key - Environment Variables
# Copy file ini menjadi .env dan isi dengan nilai yang sesuai
# JANGAN upload file .env ke GitHub!
# =============================================

# Port server (default: 3000)
PORT=3000

# Token untuk admin panel (buat string acak panjang dan kuat!)
# Contoh generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_TOKEN=GANTI_DENGAN_TOKEN_ACAK_PANJANG_DAN_KUAT

# Salt untuk hash HWID (buat string acak!)
HWID_SALT=GANTI_DENGAN_SALT_ACAK_UNIK

# Secret untuk checkpoint token (buat string acak!)
CP1_SECRET=GANTI_DENGAN_SECRET_CP1_ACAK
CP2_SECRET=GANTI_DENGAN_SECRET_CP2_ACAK

# URL base aplikasi kamu (penting untuk generate checkpoint URL)
# Development: http://localhost:3000
# Production: https://nama-project-kamu.vercel.app
BASE_URL=http://localhost:3000

# Node environment
NODE_ENV=development
